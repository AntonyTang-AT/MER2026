# 服务器资源检查报告

> 检查时间：2026-07-06  
> 用途：Issue #1 阶段 0 执行前评估

## 摘要

| 资源 | 状态 | 说明 |
|------|------|------|
| CPU | ✅ 充足 | Intel Xeon Platinum 8470Q，208 逻辑核 |
| 内存 | ✅ 充足 | 754 GiB 总量，约 680 GiB 可用 |
| 数据盘 | ⚠️ 注意 | `/root/autodl-tmp` 约 **650 GiB** 可用 |
| GPU | ❌ 不可用 | 当前节点 **未检测到 NVIDIA GPU** |
| 系统盘 | ⚠️ 偏小 | `/` 仅约 30 GiB，大文件请放 `autodl-tmp` |

## 对阶段 0 的影响

- **0.1 克隆 MERTools**：无压力，可在本机完成。
- **0.2 Conda 环境**：内存/CPU 足够；无 GPU 时仍可安装环境，但 **无法跑 CUDA 训练/推理**。
- **0.3 数据集（~729 GB）**：数据盘可能 **略小于全量数据集**，需确认 AutoDL 是否可扩容，或按需下载子集。
- **0.4 模型权重**：Qwen2.5-7B 等约 15–20 GB+，磁盘足够。

## 建议

1. **训练与推理**请在带 GPU 的 AutoDL 实例上执行（建议 ≥ 24GB 显存，官方推荐约 80GB×3 用于满 batch 训练）。
2. 本机可作为 **代码开发 + 数据/环境准备** 节点。
3. 所有大数据、模型、输出统一放在 `/root/autodl-tmp/MER2026/` 下。
